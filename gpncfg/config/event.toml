[gpncfg]
motd = '''
 ^ ^   hooot hoooot
(O,O)     from
(   )    GPN 22
-"-"-----------------

the configuration is automatically deployed.
local changes will quickly be lost. to persist
changes, update them in the nautobot instead.

this config was generated at {timestamp}
'''
output-dir = "./generated-configs"
rollback-timeout = 3
snmp-contact = "gpn-noc@lists.entropia.de"
